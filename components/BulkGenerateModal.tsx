import React from 'react';

// This component was deprecated and its content was corrupted, causing a loading error.
// It is now an empty component to fix the syntax error.
const BulkGenerateModal: React.FC = () => {
  return null;
};

export default BulkGenerateModal;
